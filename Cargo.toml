[package]
name = "first-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
dotenv = "0.15"
sqlite = "0.37"
postgres = "0.19"
postgres-native-tls = "0.5"
native-tls = "0.2"
fake = { version = "2.8", default-features = false, features = ["derive"] }
rust_decimal = { version = "1.33", features = ["db-postgres"] }
